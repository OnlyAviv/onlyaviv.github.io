---
import { Icon } from 'astro-icon/components';
import achievements from '../data/achievements.json';
import StaticLayout from '../layouts/StaticLayout.astro';
---

<StaticLayout>
  <main class="container mx-auto px-4 py-8">
    <h1 class="text-4xl font-bold mb-8 text-center">My Achievements</h1>
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
      {
        achievements.map((achievement) => (
          <div class="bg-white rounded-lg shadow-md p-6 text-center">
            <div class="flex justify-center mb-4">
              <Icon class="w-12 h-12 text-red-600" name={achievement.icon} />
            </div>
            <h3 class="text-xl font-semibold mb-2">{achievement.title}</h3>
            {achievement.url && (
              <a
                href={achievement.url}
                class="text-red-600 hover:text-red-700 font-semibold transition-colors duration-300"
              >
                Learn More
              </a>
            )}
          </div>
        ))
      }
    </div>
  </main>
</StaticLayout>
